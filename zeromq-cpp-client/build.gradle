apply plugin: 'cpp'

executables {
   zmqClient {}
}

sources {
  zmqClient {
    cpp {
      source {
        srcDir "src/main/cpp"
        include "**/*.cpp"
      }
      exportedHeaders {
        srcDir "src/main/include"
        include "**/*.hpp"
      }
    }
  }
}
